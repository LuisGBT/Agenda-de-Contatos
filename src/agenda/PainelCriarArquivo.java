package agenda;

import javax.swing.*;
import java.awt.event.ActionListener;

public class PainelCriarArquivo extends JPanel {
    private JFrame parentFrame;

    public PainelCriarArquivo(JFrame parentFrame){
    this.parentFrame = parentFrame;
    parentFrame.setSize(550,550);
    }
    
}
